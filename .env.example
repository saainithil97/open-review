# ─── Authentication ───────────────────────────────────────────────
# Option 1: Claude Max / Pro subscription (recommended for local dev)
#   Leave ANTHROPIC_API_KEY blank or unset. The Agent SDK will use
#   your Claude Code CLI session. Make sure you're logged in:
#     claude login
#
# Option 2: API key
#   Uncomment and set your key from https://platform.claude.com/
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# ─── Agent model configuration ───────────────────────────────────
# Options: opus, sonnet, haiku (defaults to sonnet)
LEAD_AGENT_MODEL=sonnet
EXPLORER_AGENT_MODEL=sonnet
SENIOR_DEV_AGENT_MODEL=sonnet

# ─── Server ──────────────────────────────────────────────────────
PORT=3001
